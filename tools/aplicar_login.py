from pathlib import Path
ROOT=Path.cwd()
if not (ROOT/'backend/app/main.py').exists() or not (ROOT/'front/app/page.tsx').exists(): raise SystemExit('ERROR: ejecutá desde la raíz de dirac-admin')
AUTH_CONTENT='from __future__ import annotations\n\nimport secrets\nfrom threading import Lock\n\nfrom fastapi import APIRouter, Header, HTTPException\nfrom pydantic import BaseModel\n\nrouter = APIRouter(prefix="/api/auth", tags=["auth"])\n\n_ALLOWED_USERS = {"victor", "luciano"}\n_PASSWORD = "admin"\n_sessions: dict[str, str] = {}\n_lock = Lock()\n\n\nclass LoginRequest(BaseModel):\n    username: str\n    password: str\n\n\n@router.post("/login")\ndef login(payload: LoginRequest):\n    username = payload.username.strip().lower()\n    if username not in _ALLOWED_USERS or payload.password != _PASSWORD:\n        raise HTTPException(status_code=401, detail="Usuario o contraseña incorrectos")\n\n    token = secrets.token_urlsafe(32)\n    with _lock:\n        _sessions[token] = username\n    return {"token": token, "username": username}\n\n\n@router.post("/logout")\ndef logout(authorization: str | None = Header(default=None)):\n    token = extract_token(authorization)\n    if token:\n        with _lock:\n            _sessions.pop(token, None)\n    return {"ok": True}\n\n\n@router.get("/me")\ndef me(authorization: str | None = Header(default=None)):\n    username = require_session(authorization)\n    return {"username": username}\n\n\ndef extract_token(authorization: str | None) -> str | None:\n    if not authorization:\n        return None\n    scheme, _, token = authorization.partition(" ")\n    if scheme.lower() != "bearer" or not token:\n        return None\n    return token\n\n\ndef require_session(authorization: str | None) -> str:\n    token = extract_token(authorization)\n    if not token:\n        raise HTTPException(status_code=401, detail="Sesión requerida")\n    with _lock:\n        username = _sessions.get(token)\n    if not username:\n        raise HTTPException(status_code=401, detail="Sesión inválida o vencida")\n    return username\n\n\ndef token_is_valid(token: str | None) -> bool:\n    if not token:\n        return False\n    with _lock:\n        return token in _sessions\n'
PAGE_CONTENT='\'use client\';\n\nimport { FormEvent, useEffect, useState } from \'react\';\nimport { Sidebar } from \'@/src/components/Sidebar\';\nimport { Dashboard } from \'@/src/components/Dashboard\';\nimport { Accounts, Clients, Finance, Reports, Services, Stock, Suppliers, Works } from \'@/src/components/Modules\';\nimport { Jobs } from \'@/src/components/Jobs\';\nimport { Planning } from \'@/src/components/Planning\';\nimport { api, clearAuthToken, getAuthToken, setAuthToken } from \'@/src/lib/api\';\n\nexport type Section=\'dashboard\'|\'accounts\'|\'jobs\'|\'planning\'|\'services\'|\'works\'|\'clients\'|\'suppliers\'|\'stock\'|\'finance\'|\'reports\';\n\nexport default function HomePage(){\n const [section,setSection]=useState<Section>(\'dashboard\');\n const [authChecking,setAuthChecking]=useState(true);\n const [user,setUser]=useState(\'\');\n const [loginError,setLoginError]=useState(\'\');\n const [loginLoading,setLoginLoading]=useState(false);\n\n useEffect(()=>{\n  const check=async()=>{\n   if(!getAuthToken()){setAuthChecking(false);return}\n   try{\n    const me=await api.get<{username:string}>(\'/api/auth/me\');\n    setUser(me.username);\n   }catch{\n    clearAuthToken();\n   }finally{\n    setAuthChecking(false);\n   }\n  };\n  void check();\n\n  const requireLogin=()=>{setUser(\'\');setAuthChecking(false)};\n  window.addEventListener(\'dirac-auth-required\',requireLogin);\n  return()=>window.removeEventListener(\'dirac-auth-required\',requireLogin);\n },[]);\n\n const doLogin=async(username:string,password:string)=>{\n  setLoginLoading(true);setLoginError(\'\');\n  try{\n   const result=await api.post<{token:string;username:string}>(\'/api/auth/login\',{username,password});\n   setAuthToken(result.token);\n   setUser(result.username);\n  }catch(e:any){\n   setLoginError(e?.message||\'No se pudo iniciar sesión\');\n  }finally{\n   setLoginLoading(false);\n  }\n };\n\n const logout=async()=>{\n  try{await api.post(\'/api/auth/logout\',{})}catch{}\n  clearAuthToken();\n  setUser(\'\');\n  setSection(\'dashboard\');\n };\n\n if(authChecking)return <div className="login-screen"><div className="login-card"><div className="login-brand">DIRAC</div><p>Verificando sesión…</p></div></div>;\n if(!user)return <LoginForm loading={loginLoading} error={loginError} onLogin={doLogin}/>;\n\n return <div className="app-shell">\n  <Sidebar section={section} onChange={setSection}/>\n  <main className="main-area">\n   <header className="topbar">\n    <div><div className="eyebrow">DIRAC · GESTIÓN INTEGRAL</div><h1>{labels[section]}</h1></div>\n    <div className="topbar-actions">\n     <span className="api-pill">● API Render</span>\n     <span className="user-pill">{user}</span>\n     <button className="ghost-button" onClick={logout}>Cerrar sesión</button>\n    </div>\n   </header>\n   {section===\'dashboard\'&&<Dashboard onNavigate={setSection}/>}\n   {section===\'accounts\'&&<Accounts/>}\n   {section===\'jobs\'&&<Jobs/>}\n   {section===\'planning\'&&<Planning/>}\n   {section===\'services\'&&<Services/>}\n   {section===\'works\'&&<Works/>}\n   {section===\'clients\'&&<Clients/>}\n   {section===\'suppliers\'&&<Suppliers/>}\n   {section===\'stock\'&&<Stock/>}\n   {section===\'finance\'&&<Finance/>}\n   {section===\'reports\'&&<Reports/>}\n  </main>\n </div>\n}\n\nfunction LoginForm({loading,error,onLogin}:{loading:boolean;error:string;onLogin:(u:string,p:string)=>Promise<void>}){\n const [username,setUsername]=useState(\'\');\n const [password,setPassword]=useState(\'\');\n\n const submit=(e:FormEvent)=>{\n  e.preventDefault();\n  if(!username.trim()||!password)return;\n  void onLogin(username,password);\n };\n\n return <div className="login-screen">\n  <form className="login-card" onSubmit={submit}>\n   <div className="login-brand">DIRAC</div>\n   <div className="login-eyebrow">GESTIÓN INTEGRAL</div>\n   <h1>Iniciar sesión</h1>\n   <p>Acceso administrativo</p>\n   <label className="login-field"><span>Usuario</span><input autoFocus autoComplete="username" value={username} onChange={e=>setUsername(e.target.value)} placeholder="Usuario"/></label>\n   <label className="login-field"><span>Contraseña</span><input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Contraseña"/></label>\n   {error&&<div className="login-error">{error}</div>}\n   <button className="primary-button login-submit" type="submit" disabled={loading||!username.trim()||!password}>{loading?\'Ingresando…\':\'Ingresar\'}</button>\n  </form>\n </div>\n}\n\nconst labels:Record<Section,string>={dashboard:\'Resumen ejecutivo\',accounts:\'Cuentas y liquidez\',jobs:\'Trabajos\',planning:\'Planificación\',services:\'Servicios\',works:\'Obras\',clients:\'Clientes\',suppliers:\'Proveedores y contratistas\',stock:\'Stock de materiales\',finance:\'Finanzas\',reports:\'Reportes y ratios\'};\n'
CSS_APPEND='\n/* LOGIN DIRAC */\n.login-screen{min-height:100vh;display:grid;place-items:center;background:linear-gradient(145deg,#eef4ff 0%,#f8fafc 48%,#edf3fb 100%);padding:24px}\n.login-card{width:min(430px,100%);background:#fff;border:1px solid #dce4ef;border-radius:22px;padding:36px;box-shadow:0 24px 70px rgba(30,58,95,.14);display:grid;gap:16px}\n.login-brand{font-size:30px;font-weight:900;letter-spacing:.12em;color:#173b70}\n.login-eyebrow{font-size:11px;letter-spacing:.18em;font-weight:800;color:#6b7a90;margin-top:-12px}\n.login-card h1{margin:8px 0 -6px;font-size:28px;color:#142238}\n.login-card>p{margin:0 0 8px;color:#718096}\n.login-field{display:grid;gap:7px}\n.login-field span{font-size:13px;font-weight:700;color:#4b5b72}\n.login-field input{height:48px;border:1px solid #d8e1ec;border-radius:10px;padding:0 13px;font-size:15px;outline:none;background:#fff}\n.login-field input:focus{border-color:#2f6ee5;box-shadow:0 0 0 3px rgba(47,110,229,.10)}\n.login-error{padding:10px 12px;border-radius:9px;background:#fff1f2;color:#b42318;font-size:13px;border:1px solid #fecdd3}\n.login-submit{height:48px;margin-top:4px}\n.user-pill{display:inline-flex;align-items:center;padding:7px 10px;border-radius:999px;background:#edf4ff;color:#2455a6;font-size:12px;font-weight:800;text-transform:capitalize}\n'
(ROOT/'backend/app/routers/auth.py').write_text(AUTH_CONTENT,encoding='utf-8')
p=ROOT/'backend/app/main.py'; t=p.read_text(encoding='utf-8')
old='from .routers.planning import router as planning_router\n'; new='from .routers.planning import router as planning_router\nfrom .routers.auth import router as auth_router, token_is_valid\n'
if old not in t: raise SystemExit('ERROR: import planning_router no encontrado')
t=t.replace(old,new,1)
old='app.add_middleware(\n    CORSMiddleware,\n    allow_origins=settings.cors_list,\n    allow_credentials=True,\n    allow_methods=["*"],\n    allow_headers=["*"],\n)\n'; new='app.add_middleware(\n    CORSMiddleware,\n    allow_origins=settings.cors_list,\n    allow_credentials=True,\n    allow_methods=["*"],\n    allow_headers=["*"],\n)\n\n\n@app.middleware("http")\nasync def require_admin_session(request, call_next):\n    path = request.url.path\n    if request.method == "OPTIONS" or not path.startswith("/api/") or path.startswith("/api/auth/"):\n        return await call_next(request)\n\n    authorization = request.headers.get("Authorization", "")\n    token = authorization[7:].strip() if authorization.lower().startswith("bearer ") else None\n    if not token_is_valid(token):\n        from fastapi.responses import JSONResponse\n        return JSONResponse(status_code=401, content={"detail": "Sesión requerida"})\n\n    return await call_next(request)\n'
if old not in t: raise SystemExit('ERROR: bloque CORS no encontrado')
t=t.replace(old,new,1)
old='app.include_router(dashboard_router)\n'
if old not in t: raise SystemExit('ERROR: include dashboard no encontrado')
t=t.replace(old,'app.include_router(auth_router)\n'+old,1)
p.write_text(t,encoding='utf-8')
p=ROOT/'front/src/lib/api.ts'; t=p.read_text(encoding='utf-8')
old="const API_KEY = process.env.NEXT_PUBLIC_API_KEY || '';\n"; new="const API_KEY = process.env.NEXT_PUBLIC_API_KEY || '';\nconst AUTH_TOKEN_KEY = 'dirac_admin_token';\n\nexport function getAuthToken() {\n  if (typeof window === 'undefined') return '';\n  return localStorage.getItem(AUTH_TOKEN_KEY) || '';\n}\n\nexport function setAuthToken(token: string) {\n  if (typeof window !== 'undefined') localStorage.setItem(AUTH_TOKEN_KEY, token);\n}\n\nexport function clearAuthToken() {\n  if (typeof window !== 'undefined') localStorage.removeItem(AUTH_TOKEN_KEY);\n}\n"
if old not in t: raise SystemExit('ERROR: API_KEY no encontrado')
t=t.replace(old,new,1)
old="  if (API_KEY) headers.set('X-API-Key', API_KEY);\n  const res = await fetch(`${API_URL}${path}`, { ...init, headers, cache: 'no-store' });\n"; new="  if (API_KEY) headers.set('X-API-Key', API_KEY);\n  const authToken = getAuthToken();\n  if (authToken) headers.set('Authorization', `Bearer ${authToken}`);\n  const res = await fetch(`${API_URL}${path}`, { ...init, headers, cache: 'no-store' });\n  if (res.status === 401 && !path.startsWith('/api/auth/')) {\n    clearAuthToken();\n    if (typeof window !== 'undefined') window.dispatchEvent(new Event('dirac-auth-required'));\n  }\n"
if old not in t: raise SystemExit('ERROR: fetch API no encontrado')
t=t.replace(old,new,1); p.write_text(t,encoding='utf-8')
(ROOT/'front/app/page.tsx').write_text(PAGE_CONTENT,encoding='utf-8')
p=ROOT/'front/app/globals.css'; css=p.read_text(encoding='utf-8')
if '/* LOGIN DIRAC */' not in css: css += CSS_APPEND
p.write_text(css,encoding='utf-8')
print('OK: login agregado para victor/luciano con contraseña admin.')